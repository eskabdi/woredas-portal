# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Environment
.env
.env.*
!.env.example

# Credentials and deploy scratch. See "Deployment credentials never enter the
# repository" in CLAUDE.md — these are the paths that have actually leaked
# tokens in projects like this one.
.claude/settings.local.json
supabase/.temp/
.supabase/
p.json
payload.json
*.pem
*.key
!public/**/*.key

# Vercel
.vercel

# woreda-manual skill build artifacts — the intermediate HTML embeds the
# Amharic fonts as base64 and is regenerated by render-pdf.mjs on every run
docs/*.render.html
.claude/skills/woreda-manual/.chrome-profile-*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
