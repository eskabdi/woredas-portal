# 03 — INSA Compliance Matrix

Every Appendix A control (INSA Technical Development Checklist, implemented as testable controls) and every Appendix B Ethiopian-locale control, with the final status, the owning agent and each agent's evidence note. Full evidence is in `findings/<agent>.md`. Generated by `raw/build-matrix.py`.

Scoring: PASS = 1, PARTIAL = 0.5, FAIL = 0; N/A excluded; UNVERIFIED excluded from the percentage and counted separately. Where several agents assessed parts of one control, the verifier's verdict wins, otherwise the worst assessed part.

## Score

| Phase | Score | Scored items | UNVERIFIED | N/A |
|---|---|---|---|---|
| A. Architecture & Design | 31.2% (2.5/8) | 8 | 0 | 0 |
| Appendix B — Ethiopian Locale | 61.5% (8/13) | 13 | 0 | 0 |
| B. Stack & Inventory | 40.0% (2/5) | 5 | 0 | 0 |
| C. Coding & Implementation | 28.6% (2/7) | 7 | 0 | 1 |
| D. Security Functionality Doc | 30.0% (1.5/5) | 5 | 0 | 0 |
| E. API Security | 25.0% (1.5/6) | 6 | 0 | 0 |
| F. Testing Scope | 50.0% (1.5/3) | 3 | 0 | 0 |
| G. Living Documentation | 0.0% (0/1) | 1 | 0 | 0 |
| H. Project-specific extensions | 35.4% (8.5/24) | 24 | 0 | 0 |
| **INSA overall (Appendix A: A–H)** | **33.1%** (19.5/59) | 59 | 0 | 1 |
| **Combined incl. Appendix B** | **38.2%** (27.5/72) | 72 | 0 | 1 |

Status totals: PASS 11, PARTIAL 33, FAIL 28, N/A 1, UNVERIFIED 0 (of 73 controls).


## A. Architecture & Design

| ID | Requirement | Status | Owner | Evidence notes (per agent) |
|---|---|---|---|---|
| A-01 | DFD Level 0: external actors + system boundary | **PARTIAL** | architecture | **architecture** PARTIAL: A project DFD L0 exists and its human actors match the 8 built-in roles, platform admin and public verifier. It omits custom roles, console-role scoping, the operator and every external service (SMTP, OSM, Google Fonts,  |
| A-02 | DFD Level 1 & 2: processes, data stores, flows | **FAIL** | architecture | **architecture** FAIL: The project L1 has 5 processes and 6 stores, against 12 processes and 13 store groups as built. Civil registration, rental, settings, provisioning, receipt verification, buckets, Vault and localStorage are missing, and n |
| A-03 | Sensitive data entry points & flows flagged and secured | **FAIL** | architecture | **architecture** FAIL: The project DFD marks 4 flows. As built there are 25 PII/financial flows, each now mapped to its control in architecture/dfd.md §3.3. Most carry at least one Weak or Missing control (e.g. WP-DB-004 read gating, WP-CRY-00 · **privacy-logging** PARTIAL: PII-flow part only (architecture owns the DFD verdict). docs/dfd.md marks 4 flows with the lock symbol and maps them to RLS/TLS. It omits PII stores and flows outside the DB: localStorage drafts and offline queue (WP-PRV |
| A-04 | Deployment architecture defined (cloud/on-prem/hybrid) | **PARTIAL** | architecture | **architecture** PARTIAL: A deployment diagram exists but is stale (6 functions, 43 tables, 9 buckets vs 8/66/10). It omits region and jurisdiction, previews, SMTP, DNS and the operator path. The as-is deployment view is in system-architecture.md · **ops-scope** PARTIAL: A deployment diagram exists but is stale (6 functions, 43 tables, 9 buckets vs 8/66/10). It omits region and jurisdiction, the Vercel Git integration and preview environment, the SMTP relay, the DNS/domain, backups, and  |
| A-05 | Component architecture: modules, service-to-service, middleware, integrations | **PARTIAL** | architecture | **architecture** PARTIAL: The project component diagram lists 6 Edge Functions and no RPC groups or third-party integrations. The auditor's component view lists all 8 functions, all 64 public functions grouped, and every external integration. · **ops-scope** PARTIAL: Inputs only; architecture agent owns the verdict. The component diagram lists '6 Edge Functions' (actual 8) and no RPC groups. External integrations missing from the diagram: Google Fonts, OSM tiles, esm.sh (Edge imports |
| A-06 | Security layers: DMZ, TLS termination, WAF, IDS/IPS in architecture | **PARTIAL** | architecture | **architecture** PARTIAL: Layers are now labelled Owned/Inherited/Missing with evidence. WAF is not evidenced (dashboard opt-in). IDS/IPS and SIEM are missing. Edge verify_jwt, TLS versions, backups and Postgres network restrictions are UNVERIFIE · **ops-scope** PARTIAL: Inputs only. Layers are labelled in the docs, but the WAF is 'dashboard opt-in' with no enablement evidence and IDS/IPS/SIEM are absent. Owned/Inherited/Absent table in findings/ops-scope.md §4 (consistent with WP-INV-00 |
| A-07 | ERD: tables, PK/FK, relationships (Users, Roles, Permissions, Transactions…) | **PARTIAL** | database | **database** PARTIAL: The repo ERD is stale (14 rental tables missing). The regenerated ERD is complete from migrations; live match is UNVERIFIED. Tables without FKs: id_card_template_field / id_card_template_field_draft (no FK to id_card_tem |
| A-08 | Sensitive fields marked; AES-256 encryption or bcrypt/Argon2 hashing enforced | **FAIL** | crypto-qr | **crypto-qr** FAIL: Encryption part only. AES-256 (pgp_sym_encrypt) under per-woreda HMAC-derived keys from a Vault root exists for resident phone/email/national_id_no, household phone/email/rent_amount, service_request.applicant_phone, pay · **database** FAIL: Sensitive columns are marked in architecture/erd.md (53). Passwords live only in auth.users (no password or secret columns in public). pgp_sym encryption exists for phone/email/national_id/amounts, but the plaintext stay |

## B. Stack & Inventory

| ID | Requirement | Status | Owner | Evidence notes (per agent) |
|---|---|---|---|---|
| B-01 | Development frameworks recorded | **PARTIAL** | inventory | **inventory** PARTIAL: All frameworks are recorded, but every one of the 11 documented versions is the package.json caret floor, not the bun.lock-resolved version CI installs (--frozen-lockfile, ci.yml:30). See WP-INV-007. |
| B-02 | All libraries/plugins + versions recorded | **PARTIAL** | inventory | **inventory** PARTIAL: A complete inventory now exists (this audit). One High advisory open (js-yaml, build-time only, WP-INV-004); no CI dependency scanning; Edge Function dependencies unpinned and not in any lockfile (WP-INV-001). Repo's own · **supplychain** PARTIAL: CVE part: inventory complete and lockfile integrity good, but one High advisory is not yet addressed or formally risk-accepted, and no automated scanning exists (WP-SUP-001/002). npm audit/ls not applicable: no package-l |
| B-03 | Third-party integrations recorded (payment, SMS, external APIs) | **PARTIAL** | inventory | **inventory** PARTIAL: Repo correctly states there is no payment gateway or SMS provider (confirmed: no SDK, URL or fetch() to one). Google Fonts and OSM are listed. Missing: esm.sh, SMTP relay provider, Vercel/Supabase as processors, GitHub A |
| B-04 | Actor types with permission boundaries | **FAIL** | authz | **authz** FAIL: Enforcement part: 9 built-in roles + custom (not 8). Documented boundaries do not match DB enforcement. Read-only roles (viewer/auditor) can write (credential.verify path WP-AZ-003; permissionless tables WP-DB-006/007/00 · **inventory** PARTIAL: Actual actor set is 9 built-in roles + tenant-defined 'custom' (not 8) + anonymous public verifier + console-role-scoped super admins. Generated docs/permissions-matrix.md matches ROLE_PERMISSIONS and default_role_perms( |
| B-05 | Security infrastructure recorded (WAF, LB, IDS/IPS, SIEM) | **PARTIAL** | inventory | **inventory** PARTIAL: TLS/DDoS/LB stated as inherited (Vercel/Supabase) with provider-level reasoning; headers owned and evidenced. WAF is 'dashboard opt-in' with no evidence of enablement; LB not explicitly recorded; IDS/IPS and SIEM stated  |

## C. Coding & Implementation

| ID | Requirement | Status | Owner | Evidence notes (per agent) |
|---|---|---|---|---|
| C-01 | SQL injection prevention: parameterized queries/ORM; no string-concatenated SQL | **PARTIAL** | appsec | **appsec** PARTIAL: No SQL injection surface: no dynamic SQL in any of the 90 migrations, RPCs use typed parameters, Edge Functions use supabase-js builders. Sort columns are allow-list mapped (SORT_COLUMN / sortColumn switch). Fails the li |
| C-02 | XSS prevention: context-aware output encoding; `dangerouslySetInnerHTML` prohibited without sanitisation | **FAIL** | verifier | **appsec** FAIL: 4 HTML sinks: print route, preview and chart style are sanitised or unused. RichTextEditor assigns DB HTML to innerHTML unsanitised (WP-APP-001, stored XSS). The CSP is not strict ('unsafe-inline', WP-APP-003). A hand-ro · **verifier** FAIL: One unsanitised innerHTML sink for stored letter HTML; CSP allows 'unsafe-inline' (WP-APP-001). |
| C-03 | CSRF protection on every state-changing request | **N/A** | appsec | **appsec** N/A: The auth transport is a bearer JWT in the Authorization header, which the browser never attaches automatically. There are no cookie-authenticated endpoints, no TanStack server functions, and no Edge Function reads cookie |
| C-04 | Cookies `Secure`, `HttpOnly`, `SameSite=Lax/Strict` | **PARTIAL** | auth-session | **auth-session** PARTIAL: Bearer tokens (access + refresh) in localStorage, not HttpOnly/Secure/SameSite cookies. INSA gap not declared in docs. Compensating controls partially present (connect-src allow-list, object-src none, form-action self) b |
| C-05 | Session timeout defined (15–30 min inactivity) | **PARTIAL** | live-verification | **auth-session** PARTIAL: Client idle logout exists and is in band, but is bypassed by closing/reopening the tab and not mounted on /set-password. Server-side inactivity timeout / time-box and JWT expiry UNVERIFIED (dashboard). Suspension does no · **live-verification** PARTIAL: Live 2026-09-25: JWT expiry is 3600 s and refresh rotation is on, but the server has no inactivity timeout or time-box (0/0; Pro-plan setting). The client-side 25-minute idle logout remains the only timeout (WP-LIVE-002) |
| C-06 | Strict allow-list input validation (regex, enums) | **FAIL** | appsec | **appsec** FAIL: Enum/status columns are DB-enforced (44 named ADD CONSTRAINT CHECKs plus inline ones), and 14 forms use Zod. FAN (16 digits), phone (+251) and household email are client-only, FAN uniqueness is advisory, free-text length |
| C-07 | Generic client errors; full stack traces logged server-side only | **PARTIAL** | appsec | **appsec** PARTIAL: PASS elements: all 8 Edge Functions return fixed strings via safeError() and log server-side, SSR crashes render a generic page, and there is no console.log of PII or tokens in src/ (only console.error of Error objects). |
| C-08 | Secure file uploads: type allow-list, scanning, outside web root, random names | **FAIL** | appsec | **appsec** FAIL: PASS: all 10 buckets private, reads through signed URLs, per-woreda path-prefix RLS (TEN-06), and 7 of 11 upload paths use UUID or timestamp object names. FAIL: 8 of 10 buckets have no server MIME/size limits, SVG or any |

## D. Security Functionality Doc

| ID | Requirement | Status | Owner | Evidence notes (per agent) |
|---|---|---|---|---|
| D-01 | Access control (RBAC/ABAC): which guard/policy enforces each endpoint | **FAIL** | authz | **authz** FAIL: The SFD exists but has no per-route/RPC/Edge matrix, and its enforcement claim is contradicted (WP-AZ-009). The matrix built here shows client guards on most portal routes (exceptions listed in authz.md §7), but 14 permi |
| D-02 | Input validation strategy per module | **PARTIAL** | appsec | **appsec** PARTIAL: An 'Input validation strategy' section exists but is platform-level, not per module, and overstates the DB layer: it says 'consistently applied' although FAN, phone and household email have no DB constraint, and it says  |
| D-03 | Session & cookie logic: duration, regeneration on login, flags | **PARTIAL** | auth-session | **auth-session** PARTIAL: Documented idle values match code; JWT expiry, refresh rotation/reuse, inactivity/time-box, password policy, MFA, lockout undocumented; file names stale. Regeneration on login: new GoTrue session per signInWithPassword ( |
| D-04 | Encryption in transit: TLS 1.2+ enforced | **PARTIAL** | live-verification | **live-verification** PARTIAL: Live 2026-09-25: the web tier passes. SSL Labs grades the two production edges A+ and A, TLS 1.2/1.3 only, forward secrecy, no legacy protocols; HSTS max-age 2 years on one edge, absent on the other's sampled response. D · **ops-scope** UNVERIFIED: Code-side criteria are met (HSTS on every SSR response, no mixed-content URLs). A live TLS/HSTS probe was blocked by the audit sandbox egress policy (and a proxied result would describe the proxy anyway). Owner to supply |
| D-05 | Logging: what is logged vs excluded (passwords, PII) | **FAIL** | privacy-logging | **privacy-logging** FAIL: A 'what is logged / never logged' section exists (docs/security-functionality.md §Logging), and console.* usage is clean (no tokens, passwords or form payloads). But: (a) the logged-event set is incomplete, with no login |

## E. API Security

| ID | Requirement | Status | Owner | Evidence notes (per agent) |
|---|---|---|---|---|
| E-01 | Request/response sample files for success and failure (200/400/401…) | **FAIL** | api-edge | **api-edge** FAIL: Auditor produced as-is samples for 13 critical endpoints at docs/audit/2026-09-24/api/samples/ (200/400/401/403/404/405/409/422/429/500 each marked emitted or not-emitted with file:line). Not yet adopted by the project. |
| E-02 | OpenAPI/Swagger spec: endpoints, headers, error codes | **FAIL** | api-edge | **api-edge** FAIL: As-is OpenAPI 3.1 spec produced at docs/audit/2026-09-24/api/openapi.yaml: 43 paths, 52 operations, 0 validation errors (raw/api-edge-openapi-validation.txt). WP-API-011. |
| E-03 | AuthN: OAuth 2.0 / JWT (short expiry + refresh) / mTLS; JWT has `exp`,`iat`; strong algorithm (e.g. RS256) | **PARTIAL** | api-edge | **api-edge** PARTIAL: Edge part PASS. JWT signing algorithm (HS256 legacy secret vs asymmetric keys), access-token lifetime and refresh-token rotation are dashboard settings - UNVERIFIED. verify_jwt state undetermined (WP-API-004). QR token u · **auth-session** UNVERIFIED: Platform JWT part only (QR token claims belong to crypto-qr). Verification code PASS: every Edge Function validates via GoTrue (signature, exp, session). Signing algorithm (legacy HS256 vs asymmetric), access-token expir · **crypto-qr** PARTIAL: QR part only. Asymmetric ES256 (P-256, stronger than RSA-2048), algorithm pinned on both ends with no header, so alg:none and HS confusion are impossible. Expiry (e) present and checked client-side (date-only). iat (t) p |
| E-04 | Every route classified Public / Private / Internal in code comments | **FAIL** | api-edge | **api-edge** FAIL: Auditor classification: 3 Public RPCs, 8 Private Edge Functions (2 self-scoped), 25 Private RPCs, 26 Internal-but-exposed RPCs, 10 Internal not exposed; 66 tables + 17 views Private (sequence tables and rate_limit_bucket |
| E-05 | Third-party keys in env/vault; webhook signatures validated | **PASS** | api-edge | **api-edge** PASS: Secret-in-history scan owned by supplychain (SEC-01 PASS). Unpinned esm.sh import is WP-INV-001. |
| E-06 | Authorization middleware verifies role/permission before processing (least privilege) | **FAIL** | verifier | **api-edge** PARTIAL: FAIL component: WP-API-001 (console RBAC not enforced), WP-API-002 (no input schema validation). DB-side read-permission gaps are WP-DB-004. · **authz** FAIL: All 8 Edge Functions verify caller, status and woreda before side effects, and the rental/fee DEFINER RPCs check permission and tenant. Failures: permissionless PostgREST writes (audit_log, kebele, 6 sequences, service_r · **verifier** FAIL: Edge Functions check role/woreda but not console scope (WP-AZ-001). |

## F. Testing Scope

| ID | Requirement | Status | Owner | Evidence notes (per agent) |
|---|---|---|---|---|
| F-01 | Structured scope table of all assets | **PARTIAL** | ops-scope | **ops-scope** PARTIAL: The repo's asset table is stale (6 functions / 42 tables; missing Storage, receipt verify, rental). The auditor produced a complete replacement asset table. WP-OPS-012. |
| F-02 | Placeholder test accounts per privilege level (Admin, User, Guest) | **PARTIAL** | ops-scope | **ops-scope** PARTIAL: The existing placeholder matrix covers 4 roles in one woreda, so isolation cannot be tested. The auditor provides a full role × 2-woreda placeholder matrix incl. status variants. WP-OPS-009. |
| F-03 | Test credentials seeded ONLY in staging | **PARTIAL** | ops-scope | **ops-scope** PARTIAL: No test users or passwords in migrations or the repo (criterion met). But there is no staging to seed into, all testing happens on production with real accounts and committed test writes, and real users' seed rows are tr |

## G. Living Documentation

| ID | Requirement | Status | Owner | Evidence notes (per agent) |
|---|---|---|---|---|
| G-01 | Every feature ships with updated DFD/ERD/SFD/API docs; missing controls implemented, not deferred | **FAIL** | inventory | **inventory** FAIL: 39 code-touching commits: 5 (13%) with any doc change, 3 (8%) with a DFD/ERD/SFD/API/architecture doc change; threshold is 80%. Rental financial core (14 tables) undocumented in ERD/DFD/architecture. No 'add security lat |

## H. Project-specific extensions

| ID | Requirement | Status | Owner | Evidence notes (per agent) |
|---|---|---|---|---|
| TEN-01 | RLS enabled (and ideally FORCED) on every `public` table | **PARTIAL** | live-verification | **database** PARTIAL: Every public table has RLS enabled in the migrations; none is FORCED. Live relrowsecurity is UNVERIFIED (Appendix C q1). · **live-verification** PARTIAL: Live 2026-09-25: RLS is enabled on 66 of 66 public tables (matches the migrations). None is FORCED. |
| TEN-02 | Every tenant table has NOT NULL `woreda_id` and policies scope by caller's woreda (from JWT/profile, not client input) | **FAIL** | verifier | **authz** PARTIAL: Agrees with the database agent. Every policy derives the tenant from get_user_woreda_id(). Client-sent woreda_id values are validated by WITH CHECK, by triggers (overrides, tenant_role pin) or by explicit Edge Function c · **database** PARTIAL: Policies derive woreda server-side (never from the client). woreda_id is NOT NULL on all tenant tables except app_user/audit_log/credential_verification_log (nullable by design). 4 child tables have no woreda_id and are  · **verifier** FAIL: Policies derive woreda server-side, but three SECURITY DEFINER paths trust a client-supplied key across tenants (WP-VER-001). |
| TEN-03 | INSERT/UPDATE policies have `WITH CHECK` preventing row migration across woredas | **PASS** | database | **database** PASS: Rows cannot be moved to another woreda through RLS. woreda_id moves on app_user/tenant_role are additionally pinned by triggers (migrations 42, 19). |
| TEN-04 | Views use `security_invoker`; SECURITY DEFINER functions pin `search_path` and re-check tenant | **FAIL** | verifier | **database** PARTIAL: Gaps where tenant re-checks are missing: rental_eligibility (WP-DB-009), get_credential_live_status (WP-DB-010), generate_resident_on_birth_approval (WP-DB-003); decrypt_pii_* check tenant but not permission. · **verifier** FAIL: search_path is pinned on all definer functions (database agent), but tenant re-check is missing in at least three. |
| TEN-05 | `anon` has no access to tenant data | **PARTIAL** | database | **database** PARTIAL: No policy admits anon or PUBLIC (all TO authenticated), so anon reads nothing from tenant tables. However anon keeps DML grants on 63 tables, and rental_eligibility() is probably anon-executable. Live Appendix C q4 is ne |
| TEN-06 | Storage objects scoped per woreda | **PARTIAL** | verifier | **database** PASS: Scoping per woreda holds. It is not permission-gated inside the tenant (WP-DB-002) and not status-gated (WP-DB-001). · **verifier** PARTIAL: Per-woreda prefix scoping holds; no permission scoping inside the tenant (WP-DB-002). |
| CLS-01 | Column-level protection for Restricted PII (FAN ID, phone, GPS, photos) by role | **FAIL** | database | **database** FAIL: No column grants or masked views. Every tenant role, including print_officer and zero-grant custom roles, reads national_id_no, ethnicity, religion, GPS and photos (WP-DB-004, WP-DB-005). |
| RBAC-01 | `P` constants ↔ `ROLE_PERMISSIONS` ↔ `role_permission` table ↔ RLS are consistent | **FAIL** | verifier | **authz** FAIL: P (80 keys) = ROLE_PERMISSIONS = default_role_perms(): 0 differences. seed.sql vs default: 3 cells x 6 woredas (credential.verify false for AUD/FC/VW, F-05 still open). Against RLS: 53 OK, 10 FSM-only verbs that also nee · **verifier** FAIL: Read keys and 4 of 5 CP keys unenforced server-side (WP-DB-004, WP-AZ-001). |
| RBAC-02 | No self-escalation: tenant_admin cannot alter own role, grant super_admin, or edit `role_permission` | **PASS** | authz | **authz** PASS: A tenant_admin cannot edit their own row (USING excludes role='tenant_admin'). They cannot mint or promote a super_admin (guard_console_role_assignment, including the NULL->NULL promotion path) or a second tenant_admin ( |
| RBAC-03 | Module toggles enforced server-side, not only nav/route | **FAIL** | authz | **authz** FAIL: Only services (letter category) is checked server-side. The credentials, civil_registration, revenue, reports, audit, approvals and rental_houses toggles are UI-only (WP-AZ-004). |
| MC-01 | Maker ≠ checker enforced in DB for every approval step | **FAIL** | verifier | **verifier** FAIL: Maker != checker compares stale actor columns after a return cycle (WP-WF-003); complaint path bypasses it for letters (WP-WF-002). · **workflows** FAIL: The DB enforces verifier != approver on entry into 'approved' for credential_request, vital_event, service_request and rental_occupancy_request. Rental also enforces requester != verifier != approver with pinned actors,  |
| MC-02 | State transitions enforced server-side (no direct status jumps via PostgREST) | **FAIL** | verifier | **verifier** FAIL: Civil events and service requests can be INSERTed at late states (WP-WF-001); non-status content unfrozen (WP-WF-004). · **workflows** PARTIAL: UPDATE status changes on 6 tables are rejected unless the (entity, from, to) triple is seeded and the caller holds the permission; system rows require a transaction-scoped GUC set only inside SECURITY DEFINER bodies (all |
| MC-03 | Waivers, revocations, deletions require privileged role + reason + audit entry | **FAIL** | workflows | **workflows** FAIL: Credential revocation: tenant_admin-only (credential.revoke reserved), non-empty reason enforced by the engine, trigger-written audit row: PASS. Fee waiver: reason >= 5 chars and a role check, but the role check spans mo |
| BL-01 | Receipt and credential numbers unique and race-safe | **PARTIAL** | crypto-qr | **crypto-qr** PARTIAL: Both credential and receipt numbers are allocated with an atomic upsert (race-safe) and carry UNIQUE(woreda_id, number). Credential numbers cannot be client-supplied (insert guard, migration 29) and are immutable (migrat · **workflows** PARTIAL: Workflow part: receipt and credential numbers come from an INSERT ... ON CONFLICT DO UPDATE ... RETURNING counter (row-locked, race-safe) and are UNIQUE per woreda. But receipts are not unique per payment and payments ar |
| BL-02 | Civil-event side effects (birth→resident, death→revoke) atomic and idempotent | **PARTIAL** | verifier | **verifier** PARTIAL: Death side effect atomic and idempotent, but only 'active' credentials revoked and deceased status reversible (WP-WF-006). · **workflows** PARTIAL: Birth -> resident and death -> revoke run in the same transaction as the system-only paid -> registered transition (atomic). Birth is idempotent (resident_id IS NULL guard + pin); 'registered' has no outbound edge. Gaps: |
| CRY-01 | RS256 private key only in Edge secrets; `kid` + rotation plan | **PARTIAL** | crypto-qr | **crypto-qr** PARTIAL: PASS on location: the private key exists only as the Edge secret HARARI_EC_PRIVATE_KEY. It is not in the working tree, the 177-commit history, the DB (no key tables or columns) or the built client bundle. Only the public |
| CRY-02 | QR payload English-only, ≤ ~1.8 KB, canonical, includes expiry/status version; verifier pins algorithm | **PARTIAL** | crypto-qr | **crypto-qr** PARTIAL: Size PASS (215-byte JSON, 374-char token, 410-char URL, well under 1.8 KB). Canonical PASS (the signature covers the exact base64url bytes, which are verified before parsing). Expiry PASS. Verifier pins algorithm PASS. S |
| CRY-03 | Revocation strategy for offline verification declared | **FAIL** | verifier | **crypto-qr** FAIL: An online check and 1-year validity exist, but the public page renders green when the registry returns no row or errors. The exact-token lookup can be forced to 'no row' by any holder, with no key (WP-CRY-001). The staff · **verifier** FAIL: Online revocation check exists but fails open on miss or error; token malleability defeats exact-match lookup (WP-CRY-001). |
| LOG-01 | Audit trail append-only, trigger-written, captures actor/woreda/action/before/after/time | **FAIL** | privacy-logging | **privacy-logging** FAIL: Append-only: PARTIAL. No UPDATE/DELETE RLS policy, so anon/authenticated cannot modify rows, but UPDATE/DELETE grants remain and service_role/owner can rewrite; there is no hash chain. Trigger-written: only resident INSE |
| PRV-01 | PII inventory with classification, access, encryption, retention | **PARTIAL** | privacy-logging | **privacy-logging** PARTIAL: This audit produced a complete inventory (24 data elements, with classification, DB readers, encryption, export/print exposure and retention). The project itself has no owner-approved inventory or classification scheme.  |
| SEC-01 | No secrets in working tree or git history; no `service_role` in frontend | **PASS** | supplychain | **supplychain** PASS: Regex fallback, since gitleaks/trufflehog are not installed; entropy-only secrets with no known prefix could evade it. The local working clone is shallow (177 commits), so history was verified against a scratch mirror. O |
| QA-01 | `tsc --noEmit` clean; lint clean; build succeeds | **PARTIAL** | build-quality | **build-quality** PARTIAL: All commands succeed. PARTIAL because lint emits 8 warnings, the typecheck scope excludes supabase/functions and scripts/ (WP-BQ-002), and cleanliness partly depends on 56 inline suppressions (WP-BQ-003). |
| RT-01 | Every dynamic segment with sibling children uses `$id.index.tsx` | **PASS** | build-quality | **build-quality** PASS: Full verdict table in build-quality.md §3. |
| OPS-01 | Separate staging and production; backups/PITR; monitoring | **FAIL** | live-verification | **live-verification** FAIL: Live 2026-09-25 and owner: Free plan, no backups, PITR off, no staging, no log drain or WAF rules. · **ops-scope** FAIL: No separate staging; backups/PITR/RPO/RTO unevidenced (likely free tier); no monitoring or alerting. WP-OPS-001, -002, -006. · **verifier** FAIL: No staging; backups/PITR unevidenced (WP-OPS-001, WP-OPS-002 needs live confirmation). |

## Appendix B — Ethiopian Locale

| ID | Requirement | Status | Owner | Evidence notes (per agent) |
|---|---|---|---|---|
| ET-01 | Ethiopian Calendar is primary display in Woreda portal; Gregorian secondary | **PARTIAL** | locale | **locale** PARTIAL: Most woreda surfaces are EC-first (12 files use EthiopianDateInput; lists, details and exports use formatEthiopian*). Exceptions: Gregorian-only list/detail/export/print-footer dates, 3 native Gregorian pickers, public Q |
| ET-02 | **No Ge'ez numerals** anywhere; EC dates use Arabic numerals 0–9 | **PASS** | locale | **locale** PASS: EC dates render as '14 መስከረም 2019' and '14/01/2019' with Arabic numerals. |
| ET-03 | EC conversion correct incl. Pagumē (5/6 days) and EC leap years | **PASS** | locale | **locale** PASS: The converter is correct. Caller-side defects are recorded separately: timezone serialisation in WP-LOC-001 (Medium) and silent invalid input in WP-LOC-008 (Low). |
| ET-04 | EC fiscal year used where fiscal periods apply (Revenue/Reports) | **FAIL** | locale | **locale** FAIL: WP-LOC-003 |
| ET-05 | Ethiopian clock where local time is shown: EtHour = ((h + 6) mod 12) or 12 — 7:00 AM → 1:00 ጠዋት; 12:00 PM → 6:00 ቀትር; 7:00 PM → 1:00 ማታ; 12:00 AM → 6:00 ለሊት | **FAIL** | locale | **locale** FAIL: Local time IS displayed, always on the Western 24-hour clock. No Ethiopian-clock conversion or period labels exist. WP-LOC-002 |
| ET-06 | Names: First + Middle (father) + Last (grandfather); short name = First + Middle; no Western first/last assumptions in forms, sorting, search, or display | **PARTIAL** | locale | **locale** PARTIAL: Three-part Amharic name required in forms; no last_name/surname anywhere. The DB does not enforce the parts, the English name is a single string, and there is no short-name helper. WP-LOC-010 |
| ET-07 | Currency always ETB (ብር), never `$`/USD | **PASS** | locale | **locale** PASS: Observation: amounts use toLocaleString() with no locale (e.g. revenue.index.tsx:452), so separators follow the browser locale, and some money columns ('መጠን / Amount', revenue.index.tsx:408) carry no ETB unit. Recommend  |
| ET-08 | Amharic fonts: Tayitu.ttf primary, Jiret.ttf secondary, with sane fallback | **PARTIAL** | locale | **locale** PARTIAL: Both fonts present and ordered Tayitu→Noto (headings) and Jiret→Noto (body). Not applied to receipt, ID card or canvas PDF output; unclassed Amharic falls through Inter; Regular-only files declared as 400-700. WP-LOC-007 |
| ET-09 | Amharic strings verbatim from approved source; no machine-approximated text | **PARTIAL** | locale | **locale** PARTIAL: No approved source exists to compare against. The glossary is a stale self-extraction (1,065 uncovered runs). Missing translations: 37 English-only toasts (WP-BQ-006). Verbatim fidelity is UNVERIFIED pending native sign- |
| ET-10 | Admin hierarchy Region → Zone → Woreda → Kebele → Gott; kebele = reference only | **PASS** | locale | **locale** PASS: No kebele-level workflow authority. Observations: the Gott level is absent and a free-text 'Sub-Woreda' is used instead (WP-LOC-013, Info). Kebele rows are writable by any staff (WP-DB-007, database agent). |
| ET-11 | Woreda IDs 1–6 and 19-kebele mapping exactly as Section 3 | **PASS** | locale | **locale** PASS: Exact match to the README spec table (the Section-3 brief itself is not in the repo; README is the only in-repo copy). woreda_id is a UUID, and the '1-6' identifier is woreda_numeric_code (UNIQUE, baseline.sql:627). Live |
| ET-12 | `+251` phone format and 16-digit FAN validation enforced client + DB | **FAIL** | locale | **locale** FAIL: Client enforces; database does not. WP-LOC-005 |
| ET-13 | Naming conventions: camelCase TS, snake_case SQL | **PASS** | locale | **locale** PASS: snake_case in TS appears only as DB column keys on Supabase rows, which is expected. |
