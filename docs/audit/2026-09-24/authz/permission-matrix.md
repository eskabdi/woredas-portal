# Authorization matrix: P -> ROLE_PERMISSIONS -> default_role_perms() -> role_permission seed -> server enforcement

Generated by `raw/authz-matrix.py` and `raw/authz-enforcement.py` (audit-authz, 2026-09-24, HEAD 9950f16e). Source of truth for "server enforcement" is the latest policy/function definition after replaying all 90 migrations (`raw/audit-database-rls-inventory.txt`, `raw/audit-database-function-bodies.sql`) plus every `workflow_transition` row seeded in migrations (LEGACY-28 rows excluded, deleted by migration 30).

- `ROLE_PERMISSIONS` (`src/config/permissions.ts:198-466`) and the latest `default_role_perms()` (`supabase/migrations/00000000000083_rental_phase5_checkpoint.sql:96-113`) are **identical for all 10 role values** (0 differences), confirming `bun run check:role-perms-drift`.
- `supabase/seed.sql` carries 1,560 `role_permission` rows for 44 keys x 6 roles x 6 woredas (header claims 1,512). It differs from the compiled default in 3 cells x 6 woredas: `credential.verify` is seeded **false** for `auditor`, `finance_clerk`, `viewer` (default true). The remaining 36 keys have no seed rows and resolve from migration backfills (`ON CONFLICT DO NOTHING`) or the default.
- Reserved (A4) keys, marked R, are excluded identically by `role_permission` INSERT/UPDATE policies, the `user_permission_override_no_locked_keys` CHECK and the `tenant_role_permission_no_reserved_keys` CHECK (`00000000000078_rental_phase3_settlement.sql:79-128`), and by `RESERVED_PERMISSION_KEYS` (`permissions.ts:186-197`).
- "Server enforcement" lists where the key is actually evaluated: `RLS table.CMD`, `fn name()` (SECURITY DEFINER RPC or trigger), `FSM entity` (a `workflow_transition.required_permission`), `edge fn`. **None** means the key is checked only in the browser (or not at all).

| Permission | R | Default roles | Seed drift | Client refs | Server enforcement (latest) | Verdict |
|---|---|---|---|---|---|---|
| `resident.create` |  | TA CR RC |  | 2 | RLS resident.INSERT | OK |
| `resident.read` |  | TA SUP CR RC FC AUD VW |  | 10 | RLS resident_document.SELECT; fn entity_read_perm_ok() | MISMATCH: base-table SELECT not gated (WP-DB-004) |
| `resident.update` |  | TA CR RC |  | 11 | RLS resident.UPDATE; RLS resident_document.DELETE; RLS resident_document.INSERT; RLS resident_document.UPDATE; fn entity_attach_perm_ok() | OK |
| `resident.delete` |  | TA |  | 0 | RLS resident.DELETE | OK |
| `household.create` |  | TA RC |  | 3 | RLS household.INSERT; RLS household_change_log.INSERT | OK |
| `household.read` |  | TA SUP CR RC FC AUD VW |  | 8 | RLS resident_document.SELECT; fn entity_read_perm_ok() | MISMATCH: base-table SELECT not gated (WP-DB-004) |
| `household.update` |  | TA RC |  | 4 | RLS household.DELETE; RLS household.UPDATE; RLS household_change_log.INSERT; RLS household_change_log.UPDATE; RLS household_location.DELETE; RLS household_location.INSERT; RLS household_location.UPDATE; fn entity_attach_perm_ok() | OK |
| `credential.issue` |  | TA CR RC |  | 5 | RLS credential_request.INSERT; RLS credential_request.UPDATE; RLS credential_request_status_history.INSERT; RLS credential_status_history.INSERT; RLS residence_credential.INSERT; RLS residence_credential.UPDATE; fn entity_attach_perm_ok() | OK |
| `credential.read` |  | TA SUP CR RC FC AUD VW PO |  | 3 | fn entity_read_perm_ok(); fn get_credential_kpis() | MISMATCH: base-table SELECT not gated (WP-DB-004) |
| `credential.print` |  | TA CR RC |  | 4 | RLS credential_print_log.UPDATE; RLS credential_status_history.INSERT; RLS residence_credential.UPDATE; edge sign-credential | OK |
| `credential.verify` |  | TA SUP CR RC FC AUD VW | VW=false, FC=false, AUD=false | 3 | RLS credential_request.UPDATE; RLS credential_request_status_history.INSERT | MISMATCH: lookup-screen key grants credential_request writes (WP-AZ-003) |
| `credential.revoke` | R | TA |  | 1 | FSM residence_credential; RLS credential_status_history.INSERT; RLS residence_credential.DELETE; RLS residence_credential.UPDATE | OK |
| `credential.renew` |  | TA |  | 0 | RLS credential_status_history.INSERT; RLS residence_credential.UPDATE | OK |
| `credential.approve` | R | TA SUP |  | 2 | FSM credential_request; RLS credential_request.DELETE; RLS credential_request.UPDATE; RLS credential_request_status_history.INSERT; RLS credential_status_history.INSERT; RLS residence_credential.UPDATE; fn entity_approve_perm_ok(); fn validate_credential_fee_amount() | OK |
| `credential.submit` |  | TA CR RC |  | 0 | FSM credential_request; fn entity_attach_perm_ok() | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.review` |  | TA CR RC |  | 0 | FSM credential_request | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.resubmit` |  | TA CR RC |  | 0 | FSM credential_request | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.return` |  | TA SUP CR RC |  | 0 | FSM credential_request | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.reject` |  | TA SUP |  | 0 | FSM credential_request | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.record_payment` |  | TA FC |  | 1 | FSM credential_request | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.preview_print` |  | TA CR RC PO |  | 2 | FSM residence_credential | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.confirm_print` |  | TA CR RC PO |  | 0 | FSM credential_request; FSM residence_credential | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.activate` |  | TA CR RC PO |  | 1 | FSM credential_request; FSM residence_credential | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.suspend` |  | TA SUP |  | 1 | FSM residence_credential | PARTIAL: FSM-only; coarse table RLS must also pass (WP-AZ-006) |
| `credential.create_request` |  | TA CR RC |  | 0 | **none** | MISMATCH: unused; `credential_request` INSERT requires `credential.issue` (baseline.sql:1567) |
| `credential.authorize_reprint` |  | TA SUP PO |  | 0 | RLS credential_print_log.INSERT | OK |
| `credential.configure_policy` | R | TA |  | 0 | RLS credential_policy.INSERT; RLS credential_policy.UPDATE | OK |
| `credential.view` |  | TA SUP CR RC FC AUD VW PO |  | 0 | **none** | MISMATCH: client-only / unenforced |
| `civil.register` |  | TA CR |  | 7 | RLS vital_event.INSERT; RLS vital_event.UPDATE | OK |
| `civil.approve` | R | TA SUP |  | 4 | FSM vital_event; RLS vital_event.DELETE; RLS vital_event.UPDATE; fn validate_credential_fee_amount() | OK |
| `civil.read` |  | TA SUP CR RC AUD VW |  | 3 | fn get_civil_kpis(); fn resolve_civil_fee() | MISMATCH: base-table SELECT not gated (WP-DB-004) |
| `civil.create_event` |  | TA CR RC |  | 0 | RLS vital_event.INSERT; RLS vital_event.UPDATE | OK |
| `civil.submit` |  | TA CR RC |  | 0 | FSM vital_event; RLS vital_event.UPDATE | OK |
| `civil.resubmit` |  | TA CR RC |  | 2 | FSM vital_event; RLS vital_event.UPDATE | OK |
| `civil.verify` |  | TA CR RC |  | 3 | FSM vital_event; RLS vital_event.UPDATE | OK |
| `civil.return` |  | TA CR RC |  | 1 | FSM vital_event; RLS vital_event.UPDATE | OK |
| `civil.reject` |  | TA SUP |  | 1 | FSM vital_event; RLS vital_event.UPDATE | OK |
| `civil.record_payment` |  | TA FC |  | 3 | FSM vital_event; RLS vital_event.UPDATE; fn resolve_civil_fee() | OK |
| `civil.view` |  | TA SUP CR RC FC AUD VW |  | 0 | **none** | MISMATCH: client-only / unenforced |
| `payment.collect` |  | TA FC |  | 2 | RLS credential_request.UPDATE; RLS credential_request_status_history.INSERT; RLS payment.INSERT; RLS payment.UPDATE; RLS receipt.INSERT; RLS receipt.UPDATE | OK |
| `payment.read` |  | TA SUP FC AUD VW |  | 0 | **none** | MISMATCH: client-only / unenforced |
| `receipt.print` |  | TA SUP FC |  | 0 | RLS receipt.INSERT; RLS receipt.UPDATE | OK |
| `report.view` |  | TA SUP AUD SA |  | 1 | **none** | MISMATCH: client-only / unenforced |
| `report.export` |  | TA SUP |  | 2 | **none** | MISMATCH: client-only / unenforced |
| `audit.view` |  | TA SUP AUD SA |  | 7 | **none** | MISMATCH: client-only / unenforced |
| `tenant.manage` | R | TA SA |  | 1 | RLS credential_print_log.DELETE; RLS fee_schedule.DELETE; RLS fee_schedule.INSERT; RLS fee_schedule.UPDATE; RLS household_change_log.DELETE; RLS kebele_rental_house.DELETE; RLS payment.DELETE; RLS receipt.DELETE; RLS rental_occupancy.DELETE; RLS rental_occupancy_request.DELETE; RLS service_request.DELETE; RLS service_request.INSERT; RLS service_request.UPDATE; RLS service_request_attachment.DELETE; RLS service_request_attachment.INSERT; RLS service_request_attachment.UPDATE; RLS service_type.DELETE; RLS service_type.INSERT; RLS service_type.UPDATE; RLS woreda_settings.DELETE; RLS woreda_settings.INSERT; RLS woreda_settings.UPDATE; fn resolve_rental_checkpoint_core(); fn validate_credential_fee_amount() | OK |
| `user.manage` | R | TA SA |  | 0 | **none** | DECORATIVE: the powers it names are gated by `is_tenant_admin()` / `is_super_admin()`, not by this key |
| `platform.manage` | R | SA |  | 0 | **none** | DECORATIVE: the powers it names are gated by `is_tenant_admin()` / `is_super_admin()`, not by this key |
| `tenant.create` | R | SA |  | 0 | **none** | DECORATIVE: the powers it names are gated by `is_tenant_admin()` / `is_super_admin()`, not by this key |
| `rental.view` |  | TA SUP RC FC AUD |  | 8 | RLS arrears_installment_charge.SELECT; RLS arrears_repayment_installment.SELECT; RLS arrears_repayment_plan.SELECT; RLS payment_reconciliation_exception.SELECT; RLS rent_account.SELECT; RLS rent_charge.SELECT; RLS rent_payment_settlement.SELECT; RLS rent_rate_history.SELECT; RLS rent_reminder.SELECT; RLS rental_payment.SELECT; fn entity_read_perm_ok(); fn get_rent_account_ledger_summary() | OK |
| `rental.create` |  | TA RC |  | 7 | FSM rental_occupancy_request; RLS kebele_rental_house.INSERT; RLS kebele_rental_house.UPDATE; RLS rental_occupancy_request.INSERT; RLS rental_occupancy_request.UPDATE; RLS rental_request_document.DELETE; RLS rental_request_document.INSERT; RLS rental_request_document.UPDATE; fn entity_attach_perm_ok() | OK |
| `rental.approve` |  | TA SUP |  | 1 | FSM rental_occupancy_request; RLS kebele_rental_house.UPDATE; RLS rental_occupancy_request.UPDATE; fn entity_approve_perm_ok(); fn provision_rent_account() | OK |
| `rental.vacate` |  | TA |  | 1 | RLS kebele_rental_house.UPDATE; RLS rental_occupancy_request.INSERT; RLS rental_occupancy_request.UPDATE | OK |
| `rental.report` |  | TA AUD |  | 1 | RLS service_request_checkpoint.SELECT; fn get_rental_arrears_aging_report(); fn get_rental_billing_collection_report(); fn get_rental_checkpoint_activity_report(); fn get_rental_plan_compliance_report(); fn get_rental_reconciliation_report() | OK (enforced in the report RPCs; no base table needs it) |
| `rental.policy.configure` | R | TA |  | 0 | RLS rental_policy.INSERT; RLS rental_policy.UPDATE | OK |
| `rental.billing` |  | TA |  | 1 | fn generate_rent_charges(); fn generate_rent_reminders(); fn provision_rent_account(); fn refresh_rent_ledger_statuses() | OK |
| `rental.collect` |  | TA RC FC |  | 4 | fn settle_arrears_installments(); fn settle_rent_payment() | OK |
| `rental.settle` |  | TA FC |  | 0 | fn resolve_reconciliation_exception() | OK |
| `rental.reverse` | R | TA |  | 1 | fn reverse_rental_payment() | OK |
| `rental.plan.create` |  | TA FC |  | 3 | FSM arrears_repayment_plan; RLS arrears_repayment_plan.UPDATE; fn create_arrears_repayment_plan() | OK |
| `rental.plan.approve` |  | TA SUP |  | 1 | FSM arrears_repayment_plan; RLS arrears_repayment_plan.UPDATE | OK |
| `rental.plan.manage` |  | TA SUP |  | 1 | FSM arrears_repayment_plan; RLS arrears_repayment_plan.UPDATE | OK |
| `revenue.view` |  | TA SUP FC AUD |  | 1 | **none** | MISMATCH: client-only / unenforced |
| `revenue.collect` |  | TA FC |  | 0 | RLS credential_request.UPDATE; RLS credential_request_status_history.INSERT; RLS payment.INSERT; RLS payment.UPDATE; RLS receipt.INSERT | OK |
| `revenue.receipt_reprint` |  | TA SUP FC |  | 2 | RLS receipt.UPDATE | OK |
| `service.create` |  | TA CR RC |  | 3 | RLS service_request.INSERT; RLS service_request.UPDATE; RLS service_request_attachment.DELETE; RLS service_request_attachment.INSERT; RLS service_request_attachment.UPDATE; fn entity_attach_perm_ok(); fn resolve_rental_checkpoint_core() | OK |
| `service.read` |  | TA SUP CR RC FC AUD VW |  | 3 | RLS service_request_checkpoint.SELECT; fn entity_read_perm_ok(); fn get_service_kpis(); fn resolve_service_fee() | MISMATCH: base-table SELECT not gated (WP-DB-004) |
| `service.verify` |  | TA SUP CR RC |  | 2 | FSM service_request; RLS service_request.UPDATE; RLS service_request_attachment.INSERT; RLS service_request_attachment.UPDATE | OK |
| `service.approve` |  | TA SUP |  | 3 | FSM service_request; RLS service_request.UPDATE; fn entity_approve_perm_ok(); fn validate_credential_fee_amount() | OK |
| `service.issue` |  | TA CR RC |  | 1 | FSM service_request; RLS service_request.UPDATE | OK |
| `service.submit` |  | TA CR RC |  | 0 | FSM service_request; RLS service_request.INSERT; RLS service_request.UPDATE; fn entity_attach_perm_ok(); fn resolve_rental_checkpoint_core() | OK |
| `service.resubmit` |  | TA CR RC |  | 1 | FSM service_request; RLS service_request.UPDATE | OK |
| `service.return` |  | TA CR RC |  | 1 | FSM service_request; RLS service_request.UPDATE | OK |
| `service.reject` |  | TA SUP |  | 1 | FSM service_request; RLS service_request.UPDATE | OK |
| `service.record_payment` |  | TA FC |  | 3 | FSM service_request; RLS service_request.UPDATE; fn resolve_service_fee() | OK |
| `service.issue_letter` |  | TA CR RC |  | 2 | FSM service_request; RLS service_request.UPDATE | OK |
| `service.complete` |  | TA CR RC |  | 1 | FSM service_request; RLS service_request.UPDATE | OK |
| `service.checkpoint_override` |  | TA SUP |  | 1 | fn enforce_rental_checkpoint() | OK |
| `complaint.manage` |  | TA SUP RC |  | 0 | RLS service_request.INSERT; RLS service_request.UPDATE; RLS service_request_attachment.DELETE; RLS service_request_attachment.INSERT; RLS service_request_attachment.UPDATE; fn resolve_rental_checkpoint_core() | OK |
| `approval.queue.view` |  | TA SUP CR RC FC PO |  | 1 | RLS approval.SELECT | OK |

Role abbreviations: TA tenant_admin, SUP supervisor, CR civil_registrar, RC registry_clerk, FC finance_clerk, AUD auditor, VW viewer, PO print_officer, SA super_admin. `custom` has no defaults (`[]`); its grants live in `tenant_role_permission`.

Note on super_admin: every policy also admits `is_super_admin()` directly, so super_admin power does not depend on this matrix. The `CP` console permissions are a separate axis and are enforced server-side only for `console.console_users.manage` (WP-AZ-001).
